import 'package:flutter/cupertino.dart';

class Totalrep extends StatelessWidget {
  const Totalrep({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}